# Copy to .env locally — never commit real values.
#
# Required
# GITHUB_TOKEN=
# GITHUB_OWNER=
# GITHUB_REPO=
# GIT_AUTHOR_NAME=
# GIT_AUTHOR_EMAIL=
# GPG_KEYID=
# GPG_PASSPHRASE=
# GPG_PUBLIC_KEY_FINGERPRINT=
#
# Optional
# GITHUB_BRANCH=main
# HTTP_LISTEN_ADDR=:8080
#
# GPG in Docker: either paste armored key OR mount a file and set path
# GPG_PRIVATE_KEY_ARMORED="-----BEGIN PGP PRIVATE KEY BLOCK-----..."
# GPG_PRIVATE_KEY_FILE=/run/secrets/gpg_private.asc
#
# GPG_PROGRAM=/usr/bin/gpg
# GIT_SIGN_COMMITS=true
